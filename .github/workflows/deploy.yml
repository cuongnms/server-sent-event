name: Server sent event CI/CD 

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: self-hosted
    if: github.actor == 'cuongnms'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Build binary
        run: |
          pwd
          go build -o server-sent-event .

      - name: Deploy app
        run: |
          pwd
          sudo /usr/bin/systemctl stop server-sent-event || true
          sudo cp ./server-sent-event /opt/go/server-sent-event
          sudo /usr/bin/systemctl start server-sent-event
